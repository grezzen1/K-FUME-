document.addEventListener('DOMContentLoaded',()=>{const base=window.driver;window.driver=function(){base();setTimeout(()=>{if(sessionStorage.kfRole!=='driver')return;let p=document.querySelector('#pc'),o=JSON.parse(localStorage.fpOrders||'[]').filter(x=>x.status==='En livraison');if(!o.length)return;p.insertAdjacentHTML('beforeend','<section class="box pad" style="margin-top:16px"><h2>Clôturer une course</h2>'+o.map(x=>'<div class="row"><span><b>'+x.id+'</b><br><small>'+x.name+' · '+x.address+'</small></span><button class="btn" onclick="finishTrip(\''+x.id+'\')">Livré · comptabiliser</button></div>').join('')+'</section>')},120)}}); 
